import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    menuContainer: {
        position: 'absolute',
        top: 80,
        right: 10,
        backgroundColor: '#fff',
        padding: 10,
        borderRadius: 5,
        zIndex: 1,
    },
    menuItem: {
        backgroundColor: '#fff',
        padding: 10,
        marginBottom: 5,
        borderRadius: 5,
    },
});

export default styles;
